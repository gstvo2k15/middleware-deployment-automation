apiVersion: v1
kind: Service
metadata:
  name: jboss-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: jboss
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: {{ .Values.service.type }}
